# Java String

A string in java are a sequential, literal representation of characters.

### Common String Methods

substring(from)
substring(from,to)
length()
charAt(index)
indexOf(char)
equals(str)
compareTo(str)
toUpperCase()
toLowerCase()
